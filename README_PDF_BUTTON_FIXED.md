# PDF to Excel floating button — fixed

The bottom-left PDF button is a real clickable `<a>` element.
It opens the local PDF to Excel app at:

http://127.0.0.1:5014

For deployment, change the `href` in `index.html` to the deployed PDF to Excel URL.
